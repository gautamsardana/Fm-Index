import subprocess
import os
import sys

BINARY = "./build/fm_index"

# TODO: add real dataset evaluations
# e.g. load a real binary dataset, run queries, report results
print("Dataset evaluations not yet implemented.")
sys.exit(0)
